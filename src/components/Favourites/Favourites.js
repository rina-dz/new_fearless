import React from 'react';
//import { userInfo } from '../../utils/userInfo';
import './Favourites.css';

function Favourites() {

    return (
        <section className='favourites-section'>
            <h1>Секция Избранного</h1>
        </section>
    )
}

export default Favourites;
